APP_ENV=development
APP_TIMEZONE=Europe/Istanbul

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=lisans_panel
DB_USER=root
DB_PASS=

# SMTP (lisans hatırlatma)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME="btDocs Lisans"
# tls | ssl | (boş = porta göre: 465→ssl, diğer→tls)
SMTP_ENCRYPTION=

# Bitime X gün kala hatırlat (virgülle)
LICENSE_REMINDER_DAYS=30,14,7,3,1
